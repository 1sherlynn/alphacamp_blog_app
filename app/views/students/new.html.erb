<div class="col-md-8 col-md-offset-2">
  <h1>This is the new page to key in a student record</h1>

	<%= form_for @student, url: students_path do |f| %>


	<% if @student.errors.any? %>
		<div id="errors"> 
			<h2> <%= pluralize(@student.errors.count, "error") %> prevented this Student record from saving </h2> 
			<ul> <% @student.errors.full_messages.each do |msg| %>
				<li> <%= msg %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	  <p>
	    <%= f.label :name %><br>
	    <%= f.text_field :name %>
	  </p>

	   <p>
	    <%= f.label :birthdate %><br>
	    <%= date_field(:student, :birthdate) %>
	  </p>

	  <p>
	    <%= f.label :email %><br>
	    <%= email_field(:student, :email) %>
	  </p>



	  



	
	 
	  <p>
	    <%= f.submit %>
	  </p>
	<% end %>
	<br>
	<br>

	<%= link_to 'Back', students_path, class: 'btn btn-warning' %>
</div>